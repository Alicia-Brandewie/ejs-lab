<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %></title>
</head>
<body>
    <%- include('./partials/nav') %>
    <h1><%= name %></h1>
    <h2>Visit us at <%= address %></h>
    <h2> Contact: <%= phone %></h>
    <h3> 
    <% if(isOpen === true) { %>
        “Yes we are open!”
    <% } else { %>
        “Sorry, we are closed.”
    <% } %>
    </h3>   

</body>
</html>
